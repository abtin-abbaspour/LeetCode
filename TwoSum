#Given an array of integers nums and an integer target, return indices of the two numbers such that they add up to target.
#You may assume that each input would have exactly one solution, and you may not use the same element twice.
#You can return the answer in any order.

class Solution(object):
    def twoSum(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: List[int]
        """
        d = dict([(value, key) for key, value in dict(enumerate(nums)).items()])
        print(d)
        for i, val in enumerate(nums):
            if target-val in d and d[target-val] != i:
                return [i, d[target-val]]   
        # Utilizing Python Dictionaries to improve the efficiency of the solution, I created a dictionary from the input list then swapped the keys and values, allowing the program to more efficiently find the matching pair of numbers
